# Simple 3-step build approach - only copy the required scripts

# Exclude everything by default
*

# Include only the 3 build scripts
!setup_ubuntu_ci.sh
!fetch_source.sh
!build_and_deploy.sh

# Include Docker files
!Dockerfile
!docker-compose.yml 